my_tuple = ('abc', 34, True, 40, 'male') # Tuple must contain more then 1 value 

print (my_tuple) # Tuple is unchangeable
print (len(my_tuple))  # len() function to check length 
print(type(my_tuple)) # check data type is <class : tuple>

# converts other iterable objects into tuples.
new_tuple = tuple([1, 2, 3]) # convert string into tuple